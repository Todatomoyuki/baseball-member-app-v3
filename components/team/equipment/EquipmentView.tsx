"use client";

/**
 * チーム道具管理（これから作る画面のプレースホルダー）。
 *
 * ⚠ 現状このコンポーネントはどこからも描画されていません。
 *   アプリメニューの「チーム道具管理」は appView を "equipment" にするだけで、
 *   元コードでも画面は切り替わりませんでした（分割時も挙動をそのまま維持しています）。
 *   有効にする方法は TeamApp.tsx のコメントを参照してください。
 */
export function EquipmentView() {
  return (
    <section className="panel">
      <h1>チーム道具管理</h1>
      <p>ここに道具管理画面を作っていきます。</p>
    </section>
  );
}
